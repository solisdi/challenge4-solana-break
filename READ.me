- Connection to the different clusters and RPC can be modified in the configuration. 
- There is a workerRPC object that handles the transactions. 
- The workerRPC is a Web Worker which runs in the background and runs independently from other scripts.
- There are separate providers that handles specific functions.
- The different providers can be accessed by any of the components inside the App component as defined in index.tsx.

- The properties of the Account object is defined in the Accounts interface.
